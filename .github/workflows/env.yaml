name: Deployment

on: [ push, pull_request ]

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
      - name: deploy
        run: echo ${{ secrets.SORT }}
      - name: deploy
        run: echo ${{ secrets.SORT }}|rev
